import os, zipfile, textwrap

base="/mnt/data/greenescapestravel"
os.makedirs(base+"/images", exist_ok=True)

index = """<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="Green Escapes Travel — explore forests, mountain roads, scenic trips and unforgettable journeys.">
  <title>Green Escapes Travel | Explore • Travel • Escape</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <header class="navbar">
    <a class="logo" href="#home">🌿 Green Escapes Travel</a>
    <button class="menu-btn" aria-label="Open menu">☰</button>
    <nav class="nav-links">
      <a href="#home">Home</a>
      <a href="#explore">Explore</a>
      <a href="#about">About</a>
      <a href="#contact">Contact</a>
    </nav>
  </header>

  <main>
    <section id="home" class="hero">
      <div class="hero-overlay">
        <p class="eyebrow">FORESTS • ROADS • TRIPS</p>
        <h1>Escape into the wild.</h1>
        <p>Discover beautiful roads, peaceful forests, mountain views and unforgettable travel moments.</p>
        <a class="btn" href="#explore">Explore Now ↓</a>
      </div>
    </section>

    <section id="explore" class="section">
      <p class="eyebrow">DISCOVER MORE</p>
      <h2>Chase views. Find peace.</h2>
      <p class="intro">Green Escapes Travel is a place for people who love nature, road trips, forests and the freedom of exploring new places.</p>

      <div class="cards">
        <article class="card">
          <div class="card-image forest"></div>
          <div class="card-body">
            <span>🌲 Nature</span>
            <h3>Into the Forest</h3>
            <p>Quiet trails, fresh air and the beauty of the wild.</p>
          </div>
        </article>
        <article class="card">
          <div class="card-image road"></div>
          <div class="card-body">
            <span>🛣️ Road Trips</span>
            <h3>Roads Worth Taking</h3>
            <p>Scenic routes and journeys where the road is part of the adventure.</p>
          </div>
        </article>
        <article class="card">
          <div class="card-image mountain"></div>
          <div class="card-body">
            <span>🏔️ Adventure</span>
            <h3>Mountain Escapes</h3>
            <p>Big views, cool air and unforgettable moments in the mountains.</p>
          </div>
        </article>
      </div>
    </section>

    <section id="about" class="about">
      <div>
        <p class="eyebrow">ABOUT US</p>
        <h2>Travel. Explore. Escape.</h2>
        <p>From winding mountain roads to hidden forest corners, Green Escapes Travel celebrates the places that make you stop, breathe and enjoy the journey.</p>
        <p>Follow along for travel videos, scenic images, road-trip inspiration and natural beauty.</p>
      </div>
      <div class="quote">“The best journeys take you somewhere you never expected.”</div>
    </section>

    <section id="contact" class="section contact">
      <p class="eyebrow">STAY CONNECTED</p>
      <h2>Follow the journey</h2>
      <p>More roads. More forests. More escapes.</p>
      <div class="socials">
        <a href="#" class="social" aria-label="YouTube">▶ YouTube</a>
        <a href="#" class="social" aria-label="Instagram">◎ Instagram</a>
        <a href="#" class="social" aria-label="Facebook">f Facebook</a>
      </div>
    </section>
  </main>

  <footer>
    <p>© <span id="year"></span> Green Escapes Travel. All rights reserved.</p>
  </footer>

  <script src="script.js"></script>
</body>
</html>
"""

css = """* { box-sizing: border-box; margin: 0; padding: 0; }
html { scroll-behavior: smooth; }
body {
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Arial, sans-serif;
  color: #183028; background: #f7faf7; line-height: 1.6;
}
.navbar {
  position: fixed; top: 0; left: 0; right: 0; z-index: 10;
  display: flex; align-items: center; justify-content: space-between;
  padding: 16px 6%; background: rgba(10,25,20,.82); backdrop-filter: blur(10px);
}
.logo { color: white; text-decoration: none; font-weight: 800; font-size: 1.05rem; }
.nav-links { display: flex; gap: 26px; }
.nav-links a { color: white; text-decoration: none; font-weight: 600; }
.nav-links a:hover { opacity: .75; }
.menu-btn { display: none; border: 0; background: none; color: white; font-size: 1.6rem; }
.hero {
  min-height: 100vh; display: flex; align-items: center;
  background:
    linear-gradient(90deg, rgba(4,20,14,.78), rgba(4,20,14,.18)),
    url("images/hero.jpg") center/cover no-repeat,
    linear-gradient(135deg,#173d2b,#6e927b);
}
.hero-overlay { max-width: 760px; padding: 120px 7% 70px; color: white; }
.eyebrow { letter-spacing: 3px; font-size: .78rem; font-weight: 800; margin-bottom: 10px; opacity: .85; }
.hero h1 { font-size: clamp(3rem, 9vw, 6.5rem); line-height: .98; margin-bottom: 24px; }
.hero p:not(.eyebrow) { max-width: 620px; font-size: 1.15rem; margin-bottom: 30px; }
.btn {
  display: inline-block; padding: 13px 23px; border-radius: 999px;
  background: white; color: #173d2b; text-decoration: none; font-weight: 800;
}
.section { max-width: 1150px; margin: auto; padding: 90px 6%; }
.section h2, .about h2 { font-size: clamp(2rem, 5vw, 3.4rem); line-height: 1.1; margin-bottom: 15px; }
.intro { max-width: 700px; margin-bottom: 42px; color: #53665e; }
.cards { display: grid; grid-template-columns: repeat(3,1fr); gap: 22px; }
.card { overflow: hidden; background: white; border-radius: 20px; box-shadow: 0 10px 30px rgba(0,0,0,.08); }
.card-image { height: 220px; background-size: cover; background-position: center; }
.card-image.forest { background-image: url("images/forest.jpg"); }
.card-image.road { background-image: url("images/road.jpg"); }
.card-image.mountain { background-image: url("images/mountain.jpg"); }
.card-body { padding: 23px; }
.card-body span { font-size: .82rem; font-weight: 800; color: #52715f; }
.card h3 { margin: 6px 0; font-size: 1.35rem; }
.card p { color: #66766f; }
.about {
  display: grid; grid-template-columns: 1.3fr .7fr; gap: 60px;
  padding: 90px 8%; background: #183d2d; color: white;
}
.about p:not(.eyebrow) { max-width: 680px; margin: 12px 0; color: #dce8e1; }
.quote {
  align-self: center; padding: 35px; border: 1px solid rgba(255,255,255,.3);
  border-radius: 20px; font-size: 1.45rem; font-weight: 700;
}
.contact { text-align: center; }
.socials { display: flex; justify-content: center; gap: 12px; flex-wrap: wrap; margin-top: 25px; }
.social {
  text-decoration: none; color: #183d2d; background: white;
  border: 1px solid #d7e1db; padding: 11px 18px; border-radius: 999px; font-weight: 700;
}
footer { text-align: center; padding: 28px 6%; background: #0d2118; color: #cbd8d1; font-size: .9rem; }

@media (max-width: 750px) {
  .navbar { padding: 14px 5%; }
  .menu-btn { display: block; }
  .nav-links {
    display: none; position: absolute; top: 62px; left: 0; right: 0;
    padding: 18px 5%; background: #10281d; flex-direction: column; gap: 14px;
  }
  .nav-links.open { display: flex; }
  .hero-overlay { padding-left: 6%; padding-right: 6%; }
  .cards { grid-template-columns: 1fr; }
  .card-image { height: 210px; }
  .about { grid-template-columns: 1fr; gap: 30px; padding: 70px 6%; }
}
"""

js = """const menuBtn = document.querySelector('.menu-btn');
const nav = document.querySelector('.nav-links');

if (menuBtn) {
  menuBtn.addEventListener('click', () => {
    nav.classList.toggle('open');
    menuBtn.textContent = nav.classList.contains('open') ? '✕' : '☰';
  });
}

document.querySelectorAll('.nav-links a').forEach(link => {
  link.addEventListener('click', () => {
    nav.classList.remove('open');
    menuBtn.textContent = '☰';
  });
});

document.getElementById('year').textContent = new Date().getFullYear();
"""

for path, data in [
    (base+"/index.html", index),
    (base+"/style.css", css),
    (base+"/script.js", js),
]:
    with open(path, "w", encoding="utf-8") as f:
        f.write(data)

# Placeholder image files: the site will use graceful gradient fallbacks if real images aren't uploaded.
# Create tiny JPEG-like? Better use SVG files but requested images/folders. Keep folder with README.
with open(base+"/images/README.txt","w",encoding="utf-8") as f:
    f.write("Add your photos here and name them hero.jpg, forest.jpg, road.jpg, and mountain.jpg. The website will use them automatically.\n")

zip_path="/mnt/data/greenescapestravel-ready.zip"
with zipfile.ZipFile(zip_path,"w",zipfile.ZIP_DEFLATED) as z:
    for root, dirs, files in os.walk(base):
        for fn in files:
            full=os.path.join(root,fn)
            z.write(full, os.path.relpath(full, "/mnt/data"))

print(zip_path)
